export interface ClientesRes {
    clientId ? : number ;
    name ? :     string | null;
    lastName ? : string | null;
    dni ? :      string | null;
    age ? :      number | null;
    id ?  :      number;
}
